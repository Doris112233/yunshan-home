import React from 'react';
import { useNavigationLoading } from './useNavigationLoading';

const NavigationLoadingHandler: React.FC = () => {
  useNavigationLoading();
  return null;
};

export default NavigationLoadingHandler;
